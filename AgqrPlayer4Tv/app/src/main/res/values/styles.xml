<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- inherit from the material theme -->
    <style name="Theme.Leanback.AgqrPlayer4Tv" parent="Theme.Leanback">
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="android:colorPrimary">@color/accent</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="android:colorPrimaryDark">@color/accent_dark</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="android:colorAccent">@color/accent</item>

        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>

        <item name="guidedStepTheme">@style/AppTheme.GuidedStep</item>
    </style>

    <style name="AppTheme.GuidedStep" parent="@style/Theme.Leanback.GuidedStep">
        <item name="guidedActionsBackground">@color/accent_dark</item>
    </style>
</resources>
